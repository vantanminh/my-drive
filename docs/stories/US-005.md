---
contract: "The owner can create, list, and revoke file or folder shares with optional expiry, password, download permission, and maximum downloads. Public access uses a high-entropy opaque token whose digest alone is stored. Password-protected shares require an expiring HttpOnly grant cookie; password attempts are rate-limited and audited without recording secrets. Revoked, expired, trashed, exhausted, or out-of-scope resources stop working immediately."
created_at: "2026-09-20T06:11:27.998524400+00:00"
e2e: 0
evidence: "cargo fmt --all, cargo check --all-targets --locked, cargo clippy --all-targets --locked -- -D warnings, cargo test --all-targets --locked (28 passed; 4 PostgreSQL tests ignored by default), and cargo test --all-targets --locked -- --ignored against fresh PostgreSQL 17 (auth/session, drive, upload/download, sharing and migration tests passed; sharing verifies owner/CSRF, token digests, Argon2 password grants and lockout, folder scope, byte ranges, download limits, expiry, explicit revoke, trash revocation and grant cleanup); docker build --tag my-drive:shares-test . produced the Linux release image."
id: US-005
integration: 1
lane: high_risk
notes: "Plan nx79uncda9cflk3x milestone 4. Keep public token and password material out of PostgreSQL plaintext, response logs, and audit details. Reuse the existing shares schema and add only the minimal grant-session storage needed for protected browser access."
platform: 1
status: implemented
title: "Secure public shares with expiry, password gates, and revocation"
type: story
unit: 1
updated_at: "2026-09-20T06:35:51.500767100+00:00"
verify: "Unit tests cover share token entropy/digests, password hashing and verification, expiry/revoke rules, password lockout, and folder-scope checks. Disposable PostgreSQL plus temporary storage integration tests cover owner isolation, create/list/revoke, public file and folder access, protected download grants, wrong-password rate limits, expiry, max-download limits, trash revocation, audit events, and range download behavior without exposing raw tokens."
---

# Secure public shares with expiry, password gates, and revocation
