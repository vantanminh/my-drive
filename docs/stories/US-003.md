---
contract: "The signed-in owner can list a folder with pagination and sorting, create folders, inspect entries, rename or move entries, trash and restore entries, and list trash. Every operation is owner-scoped, requires a valid session, and state changes require CSRF. Names cannot become paths; database constraints prevent duplicate names, cross-owner parents, and folder cycles. Trashing a folder revokes its shares across the subtree."
created_at: "2026-09-20T04:56:13.348629800+00:00"
e2e: 0
evidence: "cargo fmt --all -- --check, cargo check --all-targets --locked, cargo clippy --all-targets --locked -- -D warnings, cargo test --all-targets --locked (20 passed; 2 PostgreSQL tests ignored by default), and cargo test --all-targets --locked -- --ignored against disposable PostgreSQL 17 (drive HTTP lifecycle, auth/session flow, and migration/constraint integration all passed). The drive HTTP flow verified auth and CSRF for every mutation, owner isolation, duplicate names, cycle rejection, rename/move, search/pagination, trash/restore, subtree share revocation, and audit events."
id: US-003
integration: 1
lane: high_risk
notes: Plan nx79uncda9cflk3x milestone 2. Use drive_entries as the shared file/folder namespace and keep original names out of storage paths. A deleted folder hides descendants through ancestor checks; restoring the folder makes its non-deleted descendants visible again. Physical purge and file payload APIs remain in later slices.
platform: 0
status: implemented
title: "Authenticated drive browsing, folder metadata, and trash"
type: story
unit: 1
updated_at: "2026-09-20T05:13:40.038473100+00:00"
verify: "Unit tests cover name validation, pagination and CSRF; PostgreSQL integration covers duplicate names, cross-owner access, cycle prevention, rename/move, trash/restore, and share revocation; HTTP tests verify authentication and CSRF on every mutation."
---

# Authenticated drive browsing, folder metadata, and trash
