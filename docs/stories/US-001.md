---
contract: "The empty workspace becomes a runnable Rust/Axum service. It validates configuration, applies PostgreSQL migrations, refuses to start when the configured HDD storage mount is missing, securely bootstraps the first owner only from explicitly configured credentials, and exposes live/ready health endpoints. Metadata and auth state stay in PostgreSQL; payload bytes stay on the HDD root."
created_at: "2026-09-20T03:53:22.224562+00:00"
e2e: 1
evidence: null
id: US-001
integration: 1
lane: high_risk
notes: "Plan nx79uncda9cflk3x milestone 1. Repository audit found no application source, migrations, frontend, or deployment files. Implementation map: config -> PostgreSQL migrations -> HDD mount guard -> secure owner bootstrap -> health/API. Later slices add auth sessions, file/folder CRUD, uploads/downloads, shares, UI, cleanup, and backup/restore. Evidence: cargo fmt --all -- --check, cargo clippy --all-targets --locked -- -D warnings, cargo test --all-targets --locked (11 unit tests plus migration bundle), disposable PostgreSQL migration/constraint integration test, live and ready HTTP checks with request IDs, Argon2id bootstrap verified in PostgreSQL, Docker Linux image build, and Linux tmpfs mount/device validation including missing mount fail-closed behavior. Test-only Docker data is not production data."
platform: 1
status: implemented
title: "Foundation: Rust service, PostgreSQL schema, storage mount guard, and health"
type: story
unit: 1
updated_at: "2026-09-20T04:25:39.630269300+00:00"
verify: cargo fmt --check and cargo check pass; migration tests verify the schema; storage-guard tests prove a missing mount fails closed; health readiness reflects database and storage state; no credentials or payload bytes are logged or persisted in the repository.
---

# Foundation: Rust service, PostgreSQL schema, storage mount guard, and health
