---
contract: "The owner can create and resume uploads using persisted offsets while file bytes stream to HDD staging. Writes with a stale offset are rejected, finalization creates a generated immutable storage object and logical file version, and retries or process restarts do not corrupt completed data. Authenticated private downloads support HEAD, ETag, attachment-safe headers, and single byte ranges with 206 or 416. User filenames never form storage paths; quotas and free-space thresholds are checked before accepting bytes."
created_at: "2026-09-20T05:15:07.375917600+00:00"
e2e: 0
evidence: "cargo fmt --all, cargo check --all-targets --locked, cargo clippy --all-targets --locked -- -D warnings, cargo test --all-targets --locked (25 passed; 3 PostgreSQL tests ignored by default), and cargo test --all-targets --locked -- --ignored against fresh PostgreSQL 17 (auth/session, drive lifecycle, transfers including concurrent/idempotent finalization, restart recovery, ranges, quota, owner/CSRF checks, and migrations passed); docker build --tag my-drive:transfer-test . produced the Linux release image."
id: US-004
integration: 1
lane: high_risk
notes: Plan nx79uncda9cflk3x milestone 3. Implement a local HDD Storage abstraction first. Reconcile filesystem and PostgreSQL explicitly because they are not one transaction. Keep upload writes bounded and make staging recoverable by generated session identifiers.
platform: 1
status: implemented
title: HDD-backed resumable uploads and private range downloads
type: story
unit: 1
updated_at: "2026-09-20T06:08:32.489826400+00:00"
verify: "Unit tests cover storage key and range parsing, offset boundaries, quota checks, and safe filenames. Disposable PostgreSQL plus temporary storage integration tests verify streamed upload integrity, wrong-offset rejection, restart/resume behavior, idempotent finalization, owner authorization, HEAD, 206, 416, and HDD-only paths without buffering whole payloads."
---

# HDD-backed resumable uploads and private range downloads
