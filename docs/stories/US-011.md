---
contract: "Index existing and newly uploaded raster image versions into small WebP previews on a separately verified SSD mount. Use a persistent, deduplicated queue; one constrained background worker; bounded source bytes and pixel dimensions; metadata-stripped, orientation-correct derivatives written atomically; and authenticated owner-scoped delivery with private caching. Expose owner-only indexing progress with exact job counts, bytes, current stage, and sanitized failures, plus pause, resume, and retry controls. Use a tested decoder support registry; unsupported and active formats remain downloads. Never fall back to writing derivatives under the HDD root."
created_at: "2026-09-21T16:56:21.035326800+00:00"
e2e: 1
evidence: "Verified 2026-09-22: cargo test --all-targets --locked (51 passed); all 12 ignored PostgreSQL tests passed serially on a fresh disposable DB. Backfill tests verify 100-row batches, current/ready/active selection, recipe-specific deduplication, preservation of existing failed jobs, and repeat idempotence. Lease test races concurrent claimers, recovers an expired lease, and retries after backoff exactly once. Removing the prepared preview root makes run_once fail before claiming and creates no HDD fallback. Local Docker app/indexer processed real PNG, EXIF-orientation JPEG, and WebP uploads; authenticated viewer returned metadata-free WebP with expected card/viewer dimensions, including orientation correction. Truncated PNG became failed/decode_failed; 40001-pixel input became unsupported/resource_limit; second live worker was excluded by singleton advisory lock. Remaining: physical verified SSD unmount while the worker is running, interruption recovery during atomic publication, file-byte/pixel boundary cases, and exact progress during active work."
id: US-011
integration: 1
lane: high_risk
links:
  - IN-005
notes: "Research .firecrawl/media-indexing-research.md. Prefer libvips thumbnail/load shrinking but isolate decoder plugins and apply container CPU/memory/time limits; package presence is not proof of format/profile/animation support. This story covers static image preview indexing. Video frame samples, face embeddings/merging, and child account management remain explicit follow-on objective work."
platform: 0
status: in_progress
title: Build bounded SSD-backed image preview indexing
type: story
unit: 1
updated_at: "2026-09-21T20:22:56.520316300+00:00"
verify: "Run migrations and worker against disposable PostgreSQL and separate temporary HDD/SSD mounts. Verify new upload enqueue and existing-file backfill, deduplication by version/recipe, representative supported image fixtures and output dimensions/metadata, malformed and over-limit rejection, one-job CPU concurrency, atomic recovery/retry, pause/resume and exact owner-only progress, SSD-mount disappearance fail-closed behavior, authenticated preview access/ETags, and frontend use of WebP previews."
---

# Build bounded SSD-backed image preview indexing
